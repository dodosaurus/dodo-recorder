# Apple Developer Code Signing & Notarization Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# Required for Apple notarization (automatic upload and validation)
# Your Apple ID email (for Apple Developer account)
APPLE_ID="your-apple-id@example.com"

# App-specific password for Apple Developer (generate at appleid.apple.com)
# Go to: Sign In → App-Specific Passwords → Generate Password
# Format: abcd-efgh-ijkl-mnop
APPLE_APP_SPECIFIC_PASSWORD="your-app-specific-password"

# Your Apple Team ID (find in Apple Developer portal)
# Format: ABC123XYZ4 (10 characters)
APPLE_TEAM_ID="your-team-id"

# Optional: Only needed if Keychain auto-discovery fails
# If your Developer ID Application certificate is installed in Keychain Access,
# electron-builder will auto-discover it (recommended approach)

# Path to your code signing certificate (.p12 file)
# CSC_LINK="./certificate.p12"

# Password for your code signing certificate
# CSC_KEY_PASSWORD="your-certificate-password"

# Specify signing identity explicitly by SHA-1 hash or name
# Get hash from: security find-identity -v -p codesigning
# CSC_NAME="FF34B069A03716C6084F892B0958E1323565D23D"
# Or use the full certificate name:
# CSC_NAME="Developer ID Application: Your Name (TEAM_ID)"
